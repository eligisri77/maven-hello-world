replicaCount: 1

image:
  repository: eli/myapp   # 👈 your Docker Hub repo
  tag: "1.0.1"            # 👈 version tag from build.yml
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: myapp.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
